# Core API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Config & validation (Pydantic v2)
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Async SQLite
aiosqlite>=0.20,<1.0

# Ollama Cloud SDK (official)
ollama>=0.4,<1.0

# Auth (JWT + password hashing). Direct bcrypt, NOT passlib (unmaintained).
python-jose[cryptography]>=3.3,<4.0
bcrypt>=4.1,<5.0

# Backend 2 — LangGraph multi-agent supervisor
langgraph>=0.2,<1.0
langchain-core>=0.3,<1.0
langchain-ollama>=0.2,<1.0

# Backend 3 — DuckDB analytical engine
duckdb>=1.0,<2.0

# Excel support (Upload parsing)
openpyxl>=3.1,<4.0

# Frontend
streamlit>=1.36,<2.0
requests>=2.31,<3.0

# --- dev / test ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
